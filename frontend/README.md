# Warsaw Walking Route Planner

Simple React frontend for selecting a start point and end point on a Warsaw map.

## Run locally

```powershell
cd frontend
npm install
npm run dev
```

Then open the local address shown in the terminal, usually `http://localhost:5173/`.
